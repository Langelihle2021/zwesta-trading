# Minimal requirements for FastAPI backend to run
# Install with: pip install -r requirements-minimal.txt

# Web Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database (SQLite for development, PostgreSQL for production)
sqlalchemy==2.0.23

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6
PyJWT==2.11.0

# Utilities
python-dotenv==1.0.0
requests==2.31.0
python-dateutil==2.8.2

# Development & Testing
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# Optional: Add these when ready for full features
# psycopg2-binary==2.9.9  # PostgreSQL driver
# MetaTrader5  # MT5 trading (Windows only)
# python-binance==1.0.17  # Binance API
# twilio==8.10.0  # WhatsApp alerts
# reportlab==4.0.7  # PDF reports
