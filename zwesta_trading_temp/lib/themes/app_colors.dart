import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF0096ff);
  static const Color secondary = Color(0xFFff0066);

  // Status colors
  static const Color success = Color(0xFF00c853);
  static const Color error = Color(0xFFff3d00);
  static const Color warning = Color(0xFFffc400);

  // Background colors
  static const Color darkBg = Color(0xFF0a0e27);
  static const Color cardBg = Color(0xFF1a1f3a);
  static const Color inputBg = Color(0xFF16192b);

  // Text colors
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFb0b0c3);

  // Gradient
  static const List<Color> gradientColors = [primary, secondary];
}
